# Only needed for a split deployment (frontend and backend on different
# domains/origins), e.g. frontend on Netlify + backend on Render.
# Leave unset for local dev - Vite's dev proxy handles /api automatically.
VITE_API_BASE_URL=
